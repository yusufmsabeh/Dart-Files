void main(List<String> args) {
  print("Hello world");
  print("my name is yousef");
  // List<String> subjects = ["flutter", 'dart'];
  // List<int> marks = [90, 90];
  // Map<String, dynamic> StudentDetails = {
  //   "name": "Yusuf",
  //   "subjects": subjects,
  //   "mark": marks
  // };
  // (StudentDetails["subjects"] as List).forEach((element) {
  //   print(element);
  // });

  // StudentDetails.forEach((key, value) {
  //   print("$key : $value");
  // });

  // List<String> students = [];

  // bool Empty;

  // students.addAll(["in", "ahmed", "ali", "saeed"]);
  // Empty = students.every((element) {
  //   return element.length > 0;
  // });
  // students = students.map((element) {
  //   if (element.length > 4) {
  //     String FirstChar = element.substring(0, 1).toUpperCase();
  //     String restName = element.substring(1, element.length);
  //     return FirstChar + restName;
  //   } else {
  //     return element;
  //   }
  // }).toList();

  // if (Empty) {
  //   print(students);
  // } else {
  //   print("no process");
  // }

  // String users = "";
  // List<String> usersList = ['saeed', 'hesham', 'read'];
  // List<String> toupperList;
  // usersList = usersList.map((e) {
  //   return e.toUpperCase();
  // }).toList();

  // // int i = 0;
  // // for (String name in usersList) {
  // //   if (i != usersList.length - 1) {
  // //     users += '$name/';
  // //   } else {
  // //     users += '$name';
  // //   }
  // //   i++;
  // // }
  // print(usersList);
}
